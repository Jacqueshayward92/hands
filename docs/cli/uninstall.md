---
summary: "CLI reference for `hands uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `hands uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
hands uninstall
hands uninstall --all --yes
hands uninstall --dry-run
```
